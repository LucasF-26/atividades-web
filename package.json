{
  "name": "memorial-celeste",
  "version": "1.0.0",
  "description": "Memorial Celeste - Ídolos do Cruzeiro",
  "scripts": {
    "start": "json-server --watch db/db.json --port 3000 --static public"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
